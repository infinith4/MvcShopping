@model MvcShopping.Models.TProduct

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.DisplayFor(m => m.id)<br>
    @Html.TextBoxFor(m => m.name)<br>
    @Html.TextBoxFor(m => m.price)<br>
    @Html.TextBoxFor(m => m.cateid)<br>
    <p>
        <input type="submit" value="Save" />
    </p>
}

<div>
    @Html.ActionLink("Back to List", "Test")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}


